{
  "name": "MernstackAPP",
  "version": "1.0.0",
  "description": "Real-time Chat Application with MERN Stack",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run start --prefix backend",
    "build": "npm install && npm install --prefix backend && npm install --prefix frontend && ./frontend/node_modules/.bin/vite build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
